import { MatchValueValidatorDirective } from './match-value-validator.directive';

describe('MatchValueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchValueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
