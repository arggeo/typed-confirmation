import { MatchValueValidatorAsyncDirective } from './match-value-validator-async.directive';

describe('MatchValueValidatorAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchValueValidatorAsyncDirective();
    expect(directive).toBeTruthy();
  });
});
