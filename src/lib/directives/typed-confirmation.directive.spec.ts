import { TypedConfirmationDirective } from './typed-confirmation.directive';

describe('TypedConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedConfirmationDirective();
    expect(directive).toBeTruthy();
  });
});
