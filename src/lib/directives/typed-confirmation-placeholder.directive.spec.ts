import { TypedConfirmationPlaceholderDirective } from './typed-confirmation-placeholder.directive';

describe('TypedConfirmationPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedConfirmationPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
